import { TabClickEventDirective } from './tab-click-event.directive';

describe('TabClickEventDirective', () => {
  it('should create an instance', () => {
    const directive = new TabClickEventDirective();
    expect(directive).toBeTruthy();
  });
});
