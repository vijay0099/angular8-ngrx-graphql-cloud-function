import { MenuDirective } from './menu.directive';
describe('MenuDirective', () => {
    it('should create an instance', () => {
        const directive = new MenuDirective();
        expect(directive).toBeTruthy();
    });
});
//# sourceMappingURL=menu.directive.spec.js.map